<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Dialog.Theme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">#0099FF</item>
        <item name="android:textColorPrimary">#000000</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#000000</item>
        <item name="android:datePickerDialogTheme" tools:targetApi="lollipop">@style/Dialog.Theme</item>
        <item name="android:timePickerDialogTheme" tools:targetApi="lollipop">@style/Dialog.Theme</item>
    </style>

</resources>
